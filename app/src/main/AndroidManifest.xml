<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".DfApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DfCrafting"
        android:forceDarkAllowed="false"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DfCrafting"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notify.NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.local.dfcraftmonitor.notify.PAUSE_SYNC" />
                <action android:name="com.local.dfcraftmonitor.notify.RESUME_SYNC" />
                <action android:name="com.local.dfcraftmonitor.notify.RELOGIN" />
            </intent-filter>
        </receiver>

        <!-- 制造详情 Widget 4×1 -->
        <receiver
            android:name=".widget.CraftingDetailWidgetProvider"
            android:exported="true"
            android:label="@string/widget_crafting_detail_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/crafting_detail_widget_info" />
        </receiver>

        <!-- 今日盈亏 Widget 2×1 -->
        <receiver
            android:name=".widget.TodayProfitWidgetProvider"
            android:exported="true"
            android:label="@string/widget_today_profit_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_profit_widget_info" />
        </receiver>

        <!-- 组合 Widget 4×2 半包围式 -->
        <receiver
            android:name=".widget.CombinedWidgetProvider"
            android:exported="true"
            android:label="@string/widget_combined_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/combined_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.DaySecretWidgetProvider"
            android:exported="true"
            android:label="@string/widget_day_secret_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/day_secret_widget_info" />
        </receiver>

        <!-- Widget 刷新按钮接收器 -->
        <receiver
            android:name=".widget.WidgetRefreshReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.local.dfcraftmonitor.widget.ACTION_REFRESH" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".widget.WidgetLifecycleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".widget.DaySecretWidgetConfigureActivity"
            android:exported="true"
            android:label="@string/widget_day_secret_configure_title">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- DfApp 实现了 Configuration.Provider 提供 HiltWorkerFactory；
             必须移除 androidx.startup 默认的 WorkManagerInitializer，
             让 WorkManager 按需初始化，避免双重初始化与 Hilt 工厂丢失。
             见 https://developer.android.com/topic/libraries/architecture/workmanager/advanced/custom-configuration -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
